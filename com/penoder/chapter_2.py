"""
    昨天了解了Python的六种基本数据类型，然而Python中的变量是不需要声明类型的，只需要在使用前赋值即可，
    赋值完成之后，变量才会被创建，常说的数据类型是表示在内存当中表示的类型
    1、数值（Numbers）
    2、字符串（String）
    3、列表（List）
    4、元组（Tuple）
    5、集合（Sets）
    6、字典（Dictionaries）

    其实上面的和 Java 中的数据类型还是有些相像的，
"""

# 注释，Python 的注释分为单行注释和多行注释，单行注释以现在为例，采用 # 标记； 多行注释以上面为例，采用 三个单引号 或者是 三个双引号 括起来

"""
    这里也是多行注释
"""
